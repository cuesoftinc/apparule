// GENERATED CODE — do not modify by hand.
//
// Source: design/tokens/apparule.tokens.json (duration), pulled from the
// Figma variable collection `apparule/tokens` (docs/design.md §2/§7).
// Regenerate from mobile/flutter/ with: dart run tool/gen_tokens.dart
/// Motion durations — MI specs quote exact values (design.md §2).
abstract final class AppDurationTokens {
  static const Duration fast = Duration(milliseconds: 120);
  static const Duration base = Duration(milliseconds: 200);
  static const Duration entrance = Duration(milliseconds: 250);
  static const Duration slow = Duration(milliseconds: 300);
}
