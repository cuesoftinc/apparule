# Environment Variables

To run the authentication microservice locally or in deployment, configure the following environment variable:

Variable Name - `FIREBASE_CONFIG_PATH` 
Description - File path to the Firebase service account JSON credentials. 
Default / Fallback -  `firebase-secrets.json` 